<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Tags" decoration-bf="state=='offer_accepted'" decoration-success="state in ['offer_received', 'offer_accepted']" decoration-muted="state=='sold'">
                <field name="state" invisible="1"/>
                <field name="name"/>
                <field name="property_type_id" string="Property Type"/>
                <field name="postcode"/>
                <field name="tags_ids" string="tags" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '&gt;', self)]"/>
                <field name="facades"/>

                <filter name="filter_is_available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <!--                <group expand="1" string="Group By">-->
                <!--                    <filter string="Available" name="filter_is_available" context="{'group_by':'postcode'}"/>-->
                <!--                </group>-->
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="group_by_postcode" context="{'group_by':'postcode'}"/>
                    <filter string="Property Type" name="group_by_property_type"
                            context="{'group_by':'property_type_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="My New House">
                <header>
                    <button name="sell_property" type="object" string="Sold"
                            states="new,offer_received,offer_accepted"/>
                    <button name="cancel_deal" type="object" string="Cancel"
                            states="new,offer_received,offer_accepted"/>
                    <!--                    <button name="action_draft" states="cancel" string="Reset to Draft" type="object"/>-->
                    <!--                    <button name="action_cancel" states="draft,in_progress,ongoing" string="Cancel" type="object"/>-->
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <field name="tags_ids" string="tags" widget="many2many_tags"
                           options="{'color_field': 'color', 'no_edit_color': True}"/>
                    <group>
                        <group>
                            <field name="property_type_id" string="Property Type" options="{'no_create_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" attrs="{'invisible': [('garden', '=', False)]}"/>
                                <field name="garden_orientation" attrs="{'invisible': [('garden', '=', False)]}"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offers_ids" nolabel="1" attrs="{'readonly': [('state', 'in', ['offer_accepted', 'sold', 'canceled'])]}"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="buyer_id" string="Buyer"/>
                                <field name="seller_id" string="Seller"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_filter_is_available': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Real Estate test view
            </p>
        </field>
    </record>
</odoo>